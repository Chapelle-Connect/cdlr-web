import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Chapelle Connect — Church life, beautifully connected",
  description: "The bilingual digital home of Chapelle de la Résurrection — announcements, events, media and connection in one place.",
  other: {
    "codex-preview": "development",
  },
  icons: {
    icon: "/chapelle-connect-logo.webp",
    shortcut: "/chapelle-connect-logo.webp",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
